server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    absolute_redirect off;

    location = /api {
        return 404;
    }

    location /api/ {
        return 404;
    }

    location ^~ /lk {
        location = /lk {
            return 301 /lk/;
        }

        location /lk/ {
            try_files $uri $uri/ /lk/index.html;
        }
    }

    location = /checkout {
        return 301 /checkout/;
    }

    location ^~ /checkout/ {
        add_header Cache-Control "no-store";
        try_files $uri $uri/ /checkout/index.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    location = /assets/js/catalog.js {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }

    location = /assets/js/config.uat.js {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }

    location = /assets/js/api-client.js {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }

    location = /assets/js/cart-store.js {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }

    location = /assets/js/checkout.js {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }

    location = /assets/js/site-documents.js {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|svg|webp|avif|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
        try_files $uri =404;
    }

    location ~* \.html$ {
        add_header Cache-Control "no-store";
    }
}
